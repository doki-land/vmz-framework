<template>
  <button type="button" @click="() => (armed = !armed)">
      {{ label }}: {{ armed ? 'ON' : 'OFF' }}
    </button>
</template>

<script client>
export default class EventButton {
	public label: string = "Event";
	armed = false;
}
</script>
