<template>
  <button type="button" onClick={() => (armed = !armed)}>
      {label}: {armed ? 'ON' : 'OFF'}
    </button>
</template>

<script client>
export default class EventButton {
	public label: string = "Event";
	armed = false;
}
</script>
