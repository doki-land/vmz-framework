<template>
  <button type="button" @click="() => (liked = !liked)">
      {{ label }}: {{ liked ? '♥' : '♡' }}
    </button>
</template>

<script client>
export default class LikeButton {
	public label: string = "Like";
	liked = false;
}
</script>
