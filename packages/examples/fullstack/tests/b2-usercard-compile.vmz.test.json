{
    "schema": "vmz.test.manifest.v0",
    "id": "b2.compile.usercard",
    "title": "B2: UserCard if/each Direct",
    "modes": ["compile"],
    "program": {
        "chunkId": "components/UserCard",
        "unitName": "UserCard"
    },
    "plan": {
        "schema": "vmz.plan.v0",
        "ref": "unit.plan"
    },
    "actions": [],
    "assertions": [
        {
            "schema": "vmz.test.assertion.v0",
            "kind": "graph",
            "expect": {
                "direct": true,
                "includesAll": ["api.comment('vmz-if')", "api.comment('vmz-each"]
            }
        }
    ]
}
