# `@vmz/protocol`

Versioned wire protocol schema ids shared by `vmz` CLI, `@vmz/test`, plugins, and (via N-API) Rust `vmz-protocol`.

Rust truth: `packages/compilers/vmz-protocol`. Design: `规划设计/vmz/14` · `16` · `21`.
